// HomeScreen — 검색어 입력 전 초기 화면
// 컴포넌트 자체는 거의 비어 있다 — 검색바/워드마크/메타라인/면책라인/추천칩은
// App.jsx의 .brand-block에서 모드와 무관하게 항상 렌더링되고 CSS 트랜지션으로 모드 변환.
// HomeScreen은 results 모드 진입 시 영역을 비워두는 역할.
export default function HomeScreen() {
  return null;
}
